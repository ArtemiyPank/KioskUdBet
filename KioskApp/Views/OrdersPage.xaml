<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:KioskApp.ViewModels"
             x:Class="KioskApp.Views.OrdersPage"
             Title="Orders">

    <!-- Minimal styles for headers and details -->
    <ContentPage.Resources>
        <Style x:Key="HeaderLabelStyle"
               TargetType="Label">
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="FontAttributes"
                    Value="Bold" />
            <Setter Property="TextColor"
                    Value="Black" />
        </Style>
        <Style x:Key="DetailLabelStyle"
               TargetType="Label">
            <Setter Property="FontSize"
                    Value="12" />
            <Setter Property="TextColor"
                    Value="Black" />
        </Style>
    </ContentPage.Resources>

    <StackLayout Padding="5">
        <!-- Button to manually reload orders -->
        <Button Text="Reload Orders"
                Command="{Binding ReloadOrdersCommand}"
                Margin="0,10,0,0" />
        
        <!-- ListView for all orders -->
        <ListView ItemsSource="{Binding Orders}"
                  HasUnevenRows="True"
                  SeparatorVisibility="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame HasShadow="False"
                               Padding="5"
                               Margin="2"
                               BorderColor="LightGray">
                            <StackLayout Spacing="8">
                                <!-- Order Header with basic details -->
                                <Grid ColumnSpacing="5">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <!-- Order information -->
                                    <Label Text="Order Details"
                                           Style="{StaticResource HeaderLabelStyle}"
                                           Grid.Row="0"
                                           Grid.Column="0" />
                                    <Label Text="{Binding Status}"
                                           Style="{StaticResource DetailLabelStyle}"
                                           TextColor="DarkBlue"
                                           Grid.Row="0"
                                           Grid.Column="1"
                                           HorizontalTextAlignment="End" />
                                    <Label Text="{Binding User.FirstName} {Binding User.LastName}"
                                           Style="{StaticResource DetailLabelStyle}"
                                           Grid.Row="1"
                                           Grid.Column="0" />
                                    <Label Text="{Binding Building}"
                                           Style="{StaticResource DetailLabelStyle}"
                                           Grid.Row="1"
                                           Grid.Column="1"
                                           HorizontalTextAlignment="End" />
                                </Grid>

                                <!-- Delivery info and Room details -->
                                <Grid ColumnSpacing="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Label Text="{Binding DeliveryStartTime, StringFormat='Delivery: {0:HH:mm} - {1:HH:mm}'}"
                                           Style="{StaticResource DetailLabelStyle}"
                                           Grid.Column="0" />
                                    <Label Text="{Binding RoomNumber}"
                                           Style="{StaticResource DetailLabelStyle}"
                                           Grid.Column="1"
                                           HorizontalTextAlignment="End" />
                                </Grid>

                                <!-- Order Items Header -->
                                <Label Text="Order Items:"
                                       Style="{StaticResource HeaderLabelStyle}" />

                                <!-- CollectionView for order items in a grid layout -->
                                <CollectionView ItemsSource="{Binding OrderItems}">
                                    <CollectionView.ItemsLayout>
                                        <!-- Adjust Span below to control columns per row -->
                                        <GridItemsLayout Orientation="Vertical"
                                                         Span="2" />
                                    </CollectionView.ItemsLayout>
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Frame HasShadow="False"
                                                   Padding="5"
                                                   Margin="2"
                                                   BorderColor="LightGray"
                                                   CornerRadius="5">
                                                <StackLayout Spacing="2">
                                                    <!-- Product details -->
                                                    <Label Text="{Binding Product.Name}"
                                                           Style="{StaticResource DetailLabelStyle}"
                                                           LineBreakMode="TailTruncation" />
                                                    <Label Text="{Binding Quantity, StringFormat='Qty: {0}'}"
                                                           Style="{StaticResource DetailLabelStyle}" />
                                                    <Label Text="{Binding Product.Price, StringFormat='Price: {0:C}'}"
                                                           Style="{StaticResource DetailLabelStyle}" />
                                                </StackLayout>
                                            </Frame>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>

                                <!-- Button to update order status -->
                                <Button Text="Update Status"
                                        FontSize="12"
                                        Padding="5"
                                        Command="{Binding UpdateOrderStatusCommand, Source={RelativeSource AncestorType={x:Type vm:OrdersViewModel}}}"
                                        CommandParameter="{Binding .}"
                                        HorizontalOptions="End" />
                            </StackLayout>
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>
